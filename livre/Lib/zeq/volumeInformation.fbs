
namespace livre.zeq;

table VolumeInformation {
  eventLow:ulong;
  eventHigh:ulong;
  isBigEndian:bool;
  compCount:uint = 1;
  depth:uint;
  dataType:int;
  overlap:[uint];
  maximumBlockSize:[uint];
  minPos:[float];
  maxPos:[float];
  voxels:[uint];
  worldSize:[float];
  boundingBoxMin:[float];
  boundingBoxMax:[float];

    //std::vector< MaxMinPair > minMaxValuesForLevel;
    //std::vector< Vector3ui > levelBlockDimensions;
  worldSpacePerVoxel:float;
}

root_type VolumeInformation;
